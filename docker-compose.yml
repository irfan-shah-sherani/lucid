version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nextjs_app
    ports:
      - "80:3000" # Left side = Server Port, Right side = Container Port
    restart: always